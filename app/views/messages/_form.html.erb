<%= form_for :message, url: [:reply, conversation] do |f| %>
  <%= f.label :subject %>
  <%= f.text_field :subject %>
  
  <%= f.label :body %>
  <%= f.text_area  :body %>
  
  <div>	
  	<%= f.submit :class => "btn btn-primary" %>
  </div>	

<% end %>
