<%= render_course_subnav( 'messages') %>

<ul>
  <%= content_tag_for(:li, conversation.receipts_for(current_user)) do |receipt| %>
    <h3><%= receipt.message.subject %> </h3>
    <p> <%= receipt.message.body %> </p>
	<%= render 'messages/form', conversation: conversation %>
  <% end %>
</ul>