<div class="container-fluid">
<%= form_for @forumpost do |f| %>
  <%= f.error_messages %>
  <% if params[:topic_id] %>
	<input type="hidden" id="topic_id" name="forumpost[topic_id]" value="<%= params[:topic_id] %>" />
  <% end %>
  
<div class="row-fluid">
  <div class="span6">

    <%= f.label :content %><br />
    <%= f.text_area :content %>
  
	</br>
	<div>
	    <%= f.submit "Create Post" %>
	</div>
  </div>
<div>

<% end %>
</div>